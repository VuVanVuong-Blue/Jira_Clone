package com.jira.clone.models.enums;

public enum NotificationType {
    assigned,
    mentioned,
    status_changed,
    comment_added,
    attachment_added,
    project_invitation,
    deadline_reminder
}
